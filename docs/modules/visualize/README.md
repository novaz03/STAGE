# `evacmob.visualize`

Plotting utilities that encapsulate the notebook-style folium/matplotlib workflows.

- `copy_static_report` – copy pre-rendered HTML reports into pipeline output folders.
- `plot_hexgrid_with_inset` – recreate the SES hexagon choropleth with an inset zoom, used for quick QA during analysis.
